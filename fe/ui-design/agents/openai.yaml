interface:
  display_name: "Frontend UI Design"
  short_description: "Repository-native frontend UI design"
  default_prompt: "Use $fe-ui-design to build or refine this interface with the repository's existing design system and components."

policy:
  allow_implicit_invocation: true
